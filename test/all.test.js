/* --------------------
 * yauzl-mac module
 * Tests
 * ------------------*/

'use strict';

// Modules
const yauzl = require('../index.js');

// Tests

describe('Tests', () => {
	it('exists', () => {
		expect(yauzl).toBeObject();
	});
});
